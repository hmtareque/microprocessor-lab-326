.model small
.code

mov ax,5abh

mov cx,0
mov bx,10
q: mov dx,0
   div bx
   push dx
   inc cx
   cmp ax,0
jnz q

r: pop dx
   cmp dx,10
   jb x
   add dx,7h
   x: add dx,30h
   mov ah,02h
   int 21h
loop r

mov ah,4ch
int 21h
end
